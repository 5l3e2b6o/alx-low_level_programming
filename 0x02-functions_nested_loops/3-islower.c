#include "main.h"

/*
 * islower -> checks if is lowercase
 *
 * Return: always 1 or 0 depending on condition
 * */
int _islower(int c)
{
	if(c > 'a' &&c < 'z')
	{
		return(1)
	}

	else
	{
		return (0)
	}

}
